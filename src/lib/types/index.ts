export type EntityMetaType = {
	url: string;
	faviconWidth: number;
	faviconHeight: number;
	caption: string;
} | null;

export type Theme = 'light' | 'dark';

export type AppStoreType = {
	onScrolling: boolean;
};
